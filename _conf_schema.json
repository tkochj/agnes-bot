{
  "api_key": {
    "description": "API 密钥",
    "type": "string",
    "hint": "与 cmd_config.json 中 openai provider 指向 127.0.0.1:1241 的 key 一致",
    "default": ""
  },
  "image_model": {
    "description": "默认生图模型",
    "type": "string",
    "hint": "可用 /生图模型 查看 /设置模型 动态切换",
    "default": "agnes-image-2.1-flash"
  },
  "video_model": {
    "description": "默认生视频模型",
    "type": "string",
    "hint": "可用 /生图模型 查看 /设置模型 动态切换",
    "default": "agnes-video-v2.0"
  },
  "image_size": {
    "description": "默认图片尺寸",
    "type": "string",
    "hint": "如 1024x1024 / 1024x1792 / 1792x1024",
    "default": "1024x1024"
  },
  "custom_models": {
    "description": "用户自定义模型（英文逗号分隔）",
    "type": "string",
    "hint": "查文档后在这里添加，例如 agnes-image-2.0,my-model",
    "default": ""
  }
}
